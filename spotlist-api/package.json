{
  "name": "spotlist-api",
  "version": "1.0.0",
  "engines": {
    "node": "15.4.0"
  },
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "pretest": "node scripts/check-version",
    "test": "jest",
    "test:watch": "npm run test -- --watch",
    "integration": "cypress run",
    "prestart": "node scripts/check-version && tsc",
    "start": "node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "cypress": "^7.1.0",
    "jest": "^27.1.1",
    "ts-jest": "^27.0.5",
    "typescript": "^4.2.4"
  }
}
